/**
 * if exception
 *
 * catch:
 *   push the exception on the stack and execute the subsequent code
 *   replace with catch-caught
 *
 * catch-caught:
 *   do nothing
 */

/**
 * if no exception
 *
 * catch:
 * catch-caught:
 *   do nothing
 */
