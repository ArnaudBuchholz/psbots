/** Possible Value types */
export enum ValueType {
  boolean = 'boolean',
  integer = 'integer',
  string = 'string',
  mark = 'mark',
  operator = 'operator',
  array = 'array',
  dictionary = 'dictionary'
}
