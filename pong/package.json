{
  "name": "@psbots/pong",
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "check": "markdownlint **/*.md --fix && stylelint style.css --fix && tsc --noEmit && dpdm ./src/** -T --no-tree --no-warning && eslint src/**/*.ts --fix",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@psbots/repl": "0.0.1"
  }
}
