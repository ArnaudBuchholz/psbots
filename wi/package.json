{
  "name": "@psbots/wi",
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "start": "vite",
    "check": "markdownlint **/*.md --fix && stylelint styles.css src/styles.css --fix && tsc --noEmit && dpdm ./src/** -T --no-tree --no-warning && eslint . --fix && eslint --config ../eslint.html.mjs --fix",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@psbots/repl": "0.0.1",
    "@xterm/addon-fit": "^0.10.0",
    "@xterm/xterm": "^5.5.0"
  }
}
